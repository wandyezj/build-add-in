/**
 * The text format to use when exporting snips.
 * This is used to determine how the snip data is serialized when exporting.
 */
export enum SnipExportFormat {
    Json = "json",
    Yaml = "yaml",
}
