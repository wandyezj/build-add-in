/**
 * UI Theme
 * Only supports two themes: Light and Dark.
 * Default is Light.
 *
 * These Theme applies to the Fluent UI components and Monaco Editor.
 */
export enum Theme {
    Default = "Default",
    Dark = "Dark",
    Light = "Light",
}
